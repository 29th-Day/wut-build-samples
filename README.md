A utility for building WUT samples when DKP is not installed on the system.

## Usage

If you are on a unix system, you can just run `build.sh`. If you are not, just copy each line from `build.sh` and run it individually.
